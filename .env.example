# Roundbreak Career — example environment file. setup.sh generates the real .env.
SECRET_KEY=change-me-to-a-32-byte-urlsafe-token
BACKUP_TOKEN=change-me-to-a-32-char-urlsafe-token

# False for LAN-only HTTP installs; flip to True after adding HTTPS.
SESSION_COOKIE_SECURE=False

OBSIDIAN_VAULT_PATH=/home/hunter/obsidian-vault
